meta {
  name: Delete User By Id
  type: http
  seq: 6
}

delete {
  url: {{scheme}}://{{host}}:{{port}}/api/v1/users/:id
  body: none
  auth: inherit
}

params:path {
  id: {{userId}}
}

assert {
  res.status: eq 204
}
